/*** Common module for including in other files.** Copyright (C) 2011 Josh Koch. See Copyright Notice in LICENSE.txt*/var configFile = 'config';process.argv.forEach(function (val, index, array) {    if (val === '--config') {        configFile = array[index + 1];    }});var Common = {    config: JSON.parse(require('fs').readFileSync('./' + configFile + '.json', 'utf-8'))};module.exports = Common;